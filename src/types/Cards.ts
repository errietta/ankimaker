export type SentenceCard = {
    text: string;
    meaning: string;
    reading: string;
};

export type WritingCardData = {
  word: string;
  reading: string;
  sentence: string;
  level: string;
  meaning: string;
  diagramBase64: string | null;
};
